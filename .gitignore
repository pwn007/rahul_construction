node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local
.env.*.local
*.tsbuildinfo
.vite/
coverage/
.vercel
.env*

# Deploy bundles — env.server carries the production DB password and JWT
# secret, and lives inside backend.zip too. Rebuilt on demand; never committed.
/deploy
backend/env.server

# Uploaded media — server-side content, never in git
web_next/public/uploads/
/uploads/

import { NotFoundView } from '@/features/legal/NotFoundView';

/* Rendered inside (public)/layout.tsx, so it keeps the site chrome. */
export default function NotFound() {
  return <NotFoundView />;
}
